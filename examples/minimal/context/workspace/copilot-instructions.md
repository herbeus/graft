# Workspace overview

You are in the folder that holds several checkouts of one product. When a task
spans repositories, name the repository in every file path you mention.

- `web-app/`        - the frontend
- `payments-api/`   - the API
- `context/`        - this shared context repo (edit the instructions here)
