# CLAUDE.md - web-app

Linked from the context repo. Do not edit this file inside the checkout; it is
a symlink, so an edit here changes it for every project that links it too.

@.github/copilot-instructions.md

## Extra context for this repo

The API contract lives in the `api` repo. If a change needs both, say so
explicitly instead of guessing the other side's shape.
